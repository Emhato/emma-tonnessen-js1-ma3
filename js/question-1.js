// Question 1
// Convert the function below to an arrow function:

const division = (a, b) => a % b;

// The functin can for example be called like this:
console.log(division(3, 2));

// Or like this:
// const result = division(3, 2);
// console.log(result);